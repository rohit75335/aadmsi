apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aadmsipod.fullname" . }}-test-connection"
  labels:
    {{- include "aadmsipod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aadmsipod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
