apiVersion: v1
kind: Service
metadata:
  name: {{ include "aadmsipod.fullname" . }}
  labels:
    {{- include "aadmsipod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aadmsipod.selectorLabels" . | nindent 4 }}
